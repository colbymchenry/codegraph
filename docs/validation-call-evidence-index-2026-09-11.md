# Проверка связей вызовов и актуальности индекса — 11 сентября 2026

Ветка: `fix/call-evidence-index-freshness`. База upstream: `3ed73bc127323e63153bf6ec8354afa82ce36aaf`.

## Состав

- #1760, `2b126b6b`: повторное связывание действия store не считается собственной локальной реализацией.
- #1844, `95dccf90`: сохранение вложенных вызовов без угадывания их целей; extraction version 27.
- #1843, `a5709f47`: сравнение файлов с индексом после commit, включая добавления, удаления и переименования.
- #1842, `28ca635b`: явный CLI `sync` отклоняет устаревший extraction stamp, в том числе с `--quiet`.
- Дополнительное исправление: сигнатуры методов внутри TypeScript interface/type alias исключены из проверки единственности реализации действия store. Реальные методы классов остаются кандидатами, поэтому неоднозначность не маскируется.
- Тест MCP truncation использует две настоящие функции в разных модулях и требует отдельный маркер обрезания для каждой. Файл с тем же именем не является второй реализацией функции.

## Проверки

Linux x64, Node 24.15.0, зависимости установлены через `npm ci` из lock-файла ветки.
Native-ядро собрано через `npm run build:kernel`; debug-вывод подтвердил загрузку именно бинарника этого checkout.

- `npm run build`: успешно, включая viewer и проверку копирования 29 WASM-грамматик.
- `tsc --noEmit`, `git diff --check`: успешно.
- Итоговый профильный native-прогон: **427 passed**, 16 файлов.
- Итоговый профильный WASM-прогон (`CODEGRAPH_KERNEL=0`): **406 passed**, 15 файлов; native parity в этот прогон не включён.
- Полный native-прогон: **4630 passed, 4 failed, 9 skipped**, 268 файлов. Все четыре отказа относятся к `kernel-dart-parity.test.ts`: `torture.dart` и `TortureCtors.dart`, каждый в LF и CRLF.
- Контроль на чистом upstream `3ed73bc`: те же **4 failed / 14 passed** в Dart parity с тем же собранным бинарником. Dart-исходники и fixtures идентичны upstream. Это исходное расхождение, а не новый отказ от этой интеграции; полный suite не объявляется зелёным.

Профильный набор: `kernel-tsjs-parity`, `object-literal-methods`, `bare-call-no-method`, `resolution`, `ts-chained-receiver`, `ui-steps-api`, `ui-steps-cross-tier`, `ui-steps-api-servers`, `nextjs`, `react-native-bridge`, `sync`, `cli-sync-stale-index`, `include-ignored-config`, `git-changed-untracked-dir`, `extraction-old-git`, `mcp-callers-truncation`.

Первичная связка PR оставляла `hardReset → reset` неразрешённым: индекс содержал и `S::reset` (сигнатуру интерфейса), и функцию `reset`. После дополнительного исправления проходят внешний `useStore.getState().reset()` и внутренний `get().reset()`. Негативные проверки сохраняют отсутствие связи при одной лишь сигнатуре и при двух настоящих реализациях.

## Ограничения

Windows и macOS не запускались. Полный WASM-suite не запускался. Полное хеширование inventory увеличивает стоимость `status`; новый замер на большом репозитории в этой проверке не выполнялся. Перед использованием новых результатов существующий индекс нужно пересобрать через `codegraph index`.

Установка глобального CLI, перезапуск пользовательских MCP-процессов и обновление старых индексов в эту задачу не входят. Проверка и сборка выполнены в отдельном checkout под `~/storage`.
