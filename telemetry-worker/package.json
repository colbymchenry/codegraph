{
  "name": "codegraph-telemetry-worker",
  "private": true,
  "description": "Optional self-hosted ingest endpoint for codegraph anonymous usage telemetry",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "check": "wrangler types && tsc --noEmit && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "wrangler": "^4.36.0"
  }
}
